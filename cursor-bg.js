/*document.addEventListener('mousemove', (e) => {
    const { innerWidth, innerHeight } = window;
    //  offsets (-50px to +50px)
    const moveX = ((e.clientX / innerWidth) - 0.5) * 100;
    const moveY = ((e.clientY / innerHeight) - 0.5) * 100;
    // update background position of body
    document.body.style.backgroundPosition = `calc(50% + ${moveX}px) calc(50% + 2*${moveY}px)`;
}); 
*/